name: publish

permissions:
  contents: write

on:
  push:
    tags:
      - '*'

env:
  VSIX_NAME: vscode-operator-${{ github.ref_name }}.vsix

jobs:
  publish:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configure Git identity
        shell: bash
        run: |
          git config user.email "sumneko.hotmail.com"
          git config user.name "最萌小汐（宇宙创生）"

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '22'
          cache: npm

      - name: Compile Client
        shell: bash
        run: |
          npm ci
          npm run compile

      - name: Prune dev dependencies
        shell: bash
        run: npm prune --omit=dev

      - name: Pack vsix
        shell: bash
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          npx @vscode/vsce package "$VERSION" -o "${{ env.VSIX_NAME }}" \
            --baseContentUrl "https://github.com/${{ github.repository }}/blob/${{ github.sha }}/" \
            --baseImagesUrl "https://github.com/${{ github.repository }}/raw/${{ github.sha }}/"

      - name: Upload vsix
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.VSIX_NAME }}
          path: ${{ env.VSIX_NAME }}

      - name: Publish Release
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          name: VSCode Operator ${{ github.ref_name }}
          generate_release_notes: true
          files: |
            ${{ env.VSIX_NAME }}

      - name: Publish to VSCode Market
        shell: bash
        run: npx @vscode/vsce publish -i "${{ env.VSIX_NAME }}" -p "${{ secrets.VSCE_TOKEN }}"
